// Basically, for the CSS class of our links, "main-nav", put in the a tags
// First make a function that calls immediately via IIFE
// (Immediately Invoked Function Expression)
// This will create <a> elements under the main-nav with links to resspective pages
(function main() {
  console.log("test");
})();
